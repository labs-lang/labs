
    LABSassume({%- for item in entrycond -%}(pc[tid][{{ item.name }}] == {{ item.value.first }}){% unless forloop.last %} && {% endunless %}{%- endfor -%});

{%- if siblings.size > 0 %}    LABSassume({%- if last -%}
{%- for item in siblings -%}(pc[tid][{{ item }}] == 0){% unless forloop.last %} && {% endunless %}{%- endfor -%}
{%- else -%}
{%- for item in siblings -%}(pc[tid][{{ item }}] != 0){%- unless forloop.last -%} || {% endunless %}{%- endfor -%}
{%- endif -%});
{%- endif -%}

{%- if type == "" -%}
    LABSassume((HoutCnt[tid] == 0) & (HinCnt[tid] == 0));
{%- endif -%}
{%- for guard in guards %}
    LABSassume({{ guard }});
{%- endfor -%}