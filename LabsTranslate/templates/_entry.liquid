{%- for item in entrypoints -%}
    LABSassume({%- for val in item.values -%} pc[tid][{{ item.pc }}] == {{ val }}{% unless forloop.last %} || {% endunless %}{%- endfor-%});
{%- endfor -%}
{%- for guard in guards -%}
    LABSassume({{ guard }});
{%- endfor -%}